<% @birds.each do |bird| %>
  <li class="list-group-item" role="option" data-autocomplete-value="<%= bird.id %>" data-autocomplete-label="<%= bird.name %>">
    <%= link_to bird.name, bird %>
    <span class="badge"><%= bird.description %></span>
  </li>
<% end %>
